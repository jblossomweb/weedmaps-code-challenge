import React from 'react';
import { Wrapper, Bars } from './styles';

const LoadingBars = () => (
  <Wrapper>
    <Bars>
      <div>Loading...</div>
    </Bars>
  </Wrapper>
);

export default LoadingBars;
